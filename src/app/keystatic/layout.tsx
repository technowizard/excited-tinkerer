import Component from './component';

export const metadata = {
  description: 'Dashboard interface for managing content',
  title: 'Keystatic Admin',
};

export default function Layout() {
  return <Component />;
}
